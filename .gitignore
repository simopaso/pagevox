*.iml
.gradle
/local.properties
# The whole IDE directory: it is per-machine state, and one of its files
# records the local path to the selected emulator image.
/.idea/
# Working notes for Claude Code. Kept locally, deliberately not published.
/CLAUDE.md
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
desktop.ini
